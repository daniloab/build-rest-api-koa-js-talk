// Auth
import React from "react";
import { Flex, Text } from "rebass";
import { Root } from "../Intro";
import { Center } from "../Api";

export const Auth = () => (
  <Root>
    <Center>
      <Flex>
        <ul>
          <li>used with koa router</li>
          <li>middleware</li>
        </ul>
      </Flex>
    </Center>
  </Root>
);
